/**
 * Function that sum two numbers
 * @param a - First number
 * @param b - Second number
 * @returns the summed numbers
 */
export const sumNumbers = (a: number, b: number): number => {
  return a + b
}
