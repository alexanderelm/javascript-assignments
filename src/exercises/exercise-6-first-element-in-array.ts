/**
 * Function takes an empty array or an array of numbers and returns the first number of the array
 * @param numbers - An array of numbers only
 * @returns The first number in the array
 */
export const findFirstNumber = (numbers: number[]): number | undefined => {
  const [firstNumber] = numbers

  return firstNumber
}
