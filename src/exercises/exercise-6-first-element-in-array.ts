/**
 * Function takes and arrays of numbers and returns det first number of the array
 * @param numbers - An array of numbers only
 * @returns The first number in the array
 */
export const findFirstNumber = (numbers: number[]): number => {
  const [firstNumber] = numbers
  return firstNumber
}
