/**
 * This function checks if the parameter value is less or equal to 0
 * @param value - The value that's checked weather it's less, equal or more than 0
 * @returns True if value is less or equal 0 and false if its a valuer higher than 0
 */
export const lessOrEqual = (value: number): boolean => {
  return value <= 0
}
